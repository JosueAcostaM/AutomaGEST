@model Modelos_AutomaG.Niveles

@{
    ViewData["Title"] = "Registrar Nivel";
}

<link rel="stylesheet" href="~/css/programa-form.css" asp-append-version="true" />
@if (User.IsInRole("Super Administrador")){
<div class="utn-form-container">

    <div class="utn-form-header">
        <h1 class="utn-title">Nuevo Nivel</h1>
        <p class="utn-subtitle">Complete la información para añadir un nuevo nivel</p>
    </div>

    <div class="utn-card">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger" style="margin-bottom:1rem;">
                    @ViewBag.Error
                </div>
            }

            <div asp-validation-summary="ModelOnly" class="text-danger" style="margin-bottom:1rem;"></div>

            <div class="utn-grid">


                <div class="utn-form-item">
                    <label asp-for="nombreniv" class="utn-label">Nombre del Nivel</label>
                    <input asp-for="nombreniv" class="utn-field" placeholder="Ej. Posgrado" required />
                    <span asp-validation-for="nombreniv" class="text-danger"></span>
                </div>

                <div class="utn-footer">
                    <a asp-action="ListNiveles" class="utn-btn-cancel">
                        <i data-lucide="x" style="width:18px;"></i> Cancelar
                    </a>
                    <button type="submit" class="utn-btn-save">
                        <i data-lucide="save" style="width:18px;"></i> Guardar Nivel
                    </button>
                </div>

            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            lucide.createIcons();
        });
    </script>
}
}